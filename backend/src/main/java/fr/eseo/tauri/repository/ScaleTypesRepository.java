package fr.eseo.tauri.repository;

import fr.eseo.tauri.model.ScaleTypes;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScaleTypesRepository extends JpaRepository<ScaleTypes, Integer> {
}
